<!DOCTYPE html>
<html lang='en-US'>
<head>
  <title>TARGET</title>
    <link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Audiowide"/>
  <link rel="stylesheet" href="/css/global.css"/>
  <link rel="stylesheet" href="/css/glow.css"/>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <div>
        <div>
          <div>
            {{#if success_msg}}
            <div class=" glow alert ">{{success_msg}}</div>
            {{/if}}
            {{#if error_msg}}
            <div class="glow alert ">{{error_msg}}</div>
            {{/if}}
            {{#if error}}
            <div class="glow alert">{{error}}</div>
            {{/if}}
            {{{body}}}
          </div>
        </div>

        <footer>
            <p> 2019 TARGET, Inc.</p>
        </footer>
      <a class="btn btn-default glow" id="footer" href="/">Go Home</a>
      </div>
</body>
</html>
